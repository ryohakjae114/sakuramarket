%h1 商品一覧画面

.row
  - @foods.each do |food|
    .col-4.mt-5
      .card
        = image_tag(food.image.url, class: 'card-img-top foods__index-card-image')
        .card-body.border-top
          %h2.card-title.display-one-line.fs-5= food.name
          %p.card-text.display-one-line= "#{food.price_with_tax}円"
          %p.card-text.display-three-line= food.description
          -# 商品詳細を作ったらリンクを差し込む
          = link_to '商品詳細', '#', class: 'btn btn-primary'
